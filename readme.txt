# SSM
使用SSM+mysql数据库的方式完成的一个简易的用户管理系统

主要完成了以下功能:
1.用户注册(姓名,手机号,邮箱/字段在数据库中唯一)表单提交做校验后,后端Controller收到参数也做校验,注册要用图片验证码验证.
2.用户可以通过姓名/手机号/邮箱任一一个登陆,登陆页码只有账号和密码两个输入框,当连续错误三次时,出现图片验证码.这里是把访问id和flag(记录登 
  陆错误次数)进行绑定,以保证线程安全.
3.可以对用户完成增删查改的功能,也可以上传头像.
4.对手机号,邮箱,等等输入框用正则表达式进行验证,使用bootstrapt,具有良好的页面展示效果